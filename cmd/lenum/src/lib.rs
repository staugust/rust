#[cfg(test)]
mod xx {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4)
    }
    #[test]
    fn fp() {
        assert!(false)
    }
    #[test]
    fn fp2() {
        assert!(false)
    }
}
